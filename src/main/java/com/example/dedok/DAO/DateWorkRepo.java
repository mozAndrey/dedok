package com.example.dedok.DAO;

import com.example.dedok.entity.DateWork;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DateWorkRepo extends JpaRepository<DateWork, Long> {
}
